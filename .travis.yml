language: clojure
jdk:
- oraclejdk7
install:
- wget -O boot https://github.com/boot-clj/boot/releases/download/v2-r1/boot.sh
- chmod 755 boot
- ./boot -V
- mv ./boot /usr/local/bin
script: boot build-jar && cd example/ && boot build && java -jar target/yeti-example-1.0.0.jar 5